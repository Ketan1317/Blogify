<!DOCTYPE html>
<html lang="en">
<head>
    <%- include('./partials/head.ejs') %>
    <title>Add Blog</title>
    <style>
        body {
            font-family: 'Arial', sans-serif;
            margin: 0;
            padding: 0;
            background: linear-gradient(135deg, #e9ecef 0%, #f8f9fa 80%, #dee2e6 100%); /* Softer gradient */
            color: #2c3e50;
            min-height: 100vh;
            display: flex;
            justify-content: center;
            align-items: flex-start;
            overflow-x: hidden;
        }
        .container {
            max-width: 650px;
            margin: 50px auto;
            padding: 35px;
            background-color: #ffffff;
            border-radius: 15px;
            box-shadow: 0 8px 25px rgba(0, 0, 0, 0.12);
            animation: fadeInUp 1s ease-out;
            position: relative;
            overflow: hidden;
        }
        .container::before {
            content: '';
            position: absolute;
            top: -50%;
            left: -50%;
            width: 200%;
            height: 200%;
            background: radial-gradient(circle, rgba(52, 152, 219, 0.1) 0%, transparent 70%);
            z-index: 0;
            animation: rotateGradient 10s infinite linear;
        }
        .container > * {
            position: relative;
            z-index: 1;
        }
        .mt-3 {
            margin-top: 25px;
        }
        .mb-3 {
            margin-bottom: 25px;
        }
        .form-label {
            font-weight: 600;
            color: #34495e;
            margin-bottom: 10px;
            display: block;
            text-transform: uppercase;
            font-size: 0.9rem;
            letter-spacing: 1px;
        }
        .form-control {
            width: 100%;
            padding: 14px;
            border: 2px solid #ecf0f1;
            border-radius: 8px;
            font-size: 1rem;
            background-color: #fafafa;
            transition: border-color 0.3s ease, box-shadow 0.3s ease, transform 0.2s ease;
        }
        .form-control:focus {
            border-color: #3498db;
            box-shadow: 0 0 10px rgba(52, 152, 219, 0.4);
            transform: scale(1.01);
            outline: none;
        }
        textarea.form-control {
            min-height: 180px;
            resize: vertical;
        }
        .btn-primary {
            width: 100%;
            padding: 14px;
            background: linear-gradient(45deg, #3498db, #8e44ad);
            border: none;
            border-radius: 8px;
            color: #fff;
            font-size: 1.15rem;
            font-weight: 600;
            cursor: pointer;
            text-transform: uppercase;
            transition: transform 0.3s ease, background 0.3s ease;
            box-shadow: 0 4px 15px rgba(52, 152, 219, 0.3);
        }
        .btn-primary:hover {
            background: linear-gradient(45deg, #2980b9, #6c3483);
            transform: translateY(-3px);
            box-shadow: 0 6px 20px rgba(52, 152, 219, 0.4);
        }
        .btn-primary:active {
            transform: translateY(0);
        }

        /* Animations */
        @keyframes fadeInUp {
            from { opacity: 0; transform: translateY(30px); }
            to { opacity: 1; transform: translateY(0); }
        }
        @keyframes rotateGradient {
            0% { transform: rotate(0deg); }
            100% { transform: rotate(360deg); }
        }
    </style>
</head>
<body>
    <%- include('./partials/nav.ejs') %>
    <div class="container mt-3">
        <h2 class="text-center mb-4" style="color: #3498db; font-weight: 700; animation: fadeIn 1s ease-in-out;">Create New Blog</h2>
        <form action="/blog" method="post" enctype="multipart/form-data">
            <div class="mb-3">
                <label class="form-label" for="CoverImage">Cover Image:</label>
                <input type="file" class="form-control mb-4" name="CoverImage" id="CoverImage" required>
                <label class="form-label" for="title">Blog Title:</label>
                <input type="text" class="form-control" name="title" id="title" placeholder="Enter blog title" required>
                <label class="form-label" for="body">Blog Content:</label>
                <textarea name="body" id="body" class="form-control" placeholder="Write your blog content here" required></textarea>
            </div>
            <div class="mt-3">
                <input type="submit" class="btn btn-primary" name="submit" id="submit" value="Publish Blog">
            </div>
        </form>
    </div>
    <%- include('./partials/scripts.ejs') %>
</body>
</html>